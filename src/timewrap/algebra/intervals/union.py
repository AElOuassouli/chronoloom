from timewrap.models import TimeInterval


# TODO: implement this function
def interval_union(
    interval_a: TimeInterval, interval_b: TimeInterval
) -> list[TimeInterval]:
    return [interval_a, interval_b]
    return [interval_a, interval_b]
